# Git Practice 🔧

Practicing Git fundamentals as part of my Linux/Cloud Engineer learning journey.

## What I practiced
- `git init` — initialize a repository
- `git add` — stage changes
- `git commit` — save snapshots with messages
- `git log` — view commit history
- `git remote add` — connect to GitHub
- `git push` — upload to GitHub

## Files
- `notes.txt` — simple test file used to practice the git workflow
